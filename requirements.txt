--only-binary=:all:

# Build tooling (stable pins for uv/pip)
pip==24.2
setuptools==74.1.2
wheel==0.44.0
build==1.2.1

# Core scientific stack (3.13-ready)
numpy==2.1.3          # 2.1 adds Python 3.13 support
scipy>=1.14.1,<1.16   # 1.14.1+ ships 3.13 wheels
pandas==2.2.3         # first pandas release with 3.13 wheels

# ML (no PyTorch on 3.13; use scikit-learn 1.6+ which supports 3.13)
scikit-learn>=1.6,<1.7

# App + viz
streamlit==1.36.0
altair==5.3.0

# Utilities
tqdm==4.66.4
requests==2.32.3
python-dateutil==2.9.0.post0
pytz==2024.1
